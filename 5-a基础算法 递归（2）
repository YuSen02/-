题目：递归的计算n的阶乘。
＃include<stdio.h>
int fun(int n);
int main()
{
int x,y,n;
printf("请输入第n项的项数：")；
scanf("%d",&n);
printf("%d的阶乘为 %d",n,fun(n));
return 0;
}
int fun(int n)
{
if(n==1)
return 1;
else
return n*fun(n-1);
}
